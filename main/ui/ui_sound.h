// main/ui/ui_sound.h —— 界面提示音：短促、可静音、不阻塞界面。
//
// 番茄钟阶段切换、作息节点切换与计时器到点共用同一段提示音。播放放在一次性任务里，
// 不占用 LVGL 任务与按键回调的时间；设置里静音时不发声，音量取设置里的档位。
#pragma once

// 播放一次提示音。音频未初始化或已静音时静默返回。重复调用会被合并成一次。
void ui_sound_beep(void);
